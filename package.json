{
  "name": "local-voice-assistant-gemini",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:backend": "cd backend && uv run ruff check .",
    "lint:frontend": "cd frontend && npm run lint"
  }
}
